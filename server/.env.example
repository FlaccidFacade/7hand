# 7hand Server Environment Configuration

# Server Configuration
PORT=8080
ENVIRONMENT=development

# GameLift Configuration
# Set to 'true' to enable GameLift Server SDK integration
ENABLE_GAMELIFT=false

# GameLift Fleet Configuration (required when ENABLE_GAMELIFT=true)
GAME_FLEET_ID=local-fleet
AWS_REGION=us-east-1

# GameLift Anywhere Configuration (only for Anywhere fleets)
# GAMELIFT_WEBSOCKET_URL=wss://your-websocket-url
# GAMELIFT_PROCESS_ID=your-process-id
# GAMELIFT_HOST_ID=your-host-id
# GAMELIFT_AUTH_TOKEN=your-auth-token

# Notes:
# - For local development, leave ENABLE_GAMELIFT=false
# - For managed EC2/container fleets, set ENABLE_GAMELIFT=true (SDK will auto-detect credentials)
# - For Anywhere fleets, set ENABLE_GAMELIFT=true and provide WebSocket URL and credentials
# - All four ProcessReady callbacks are implemented (OnStartGameSession, OnProcessTerminate, 
#   OnHealthCheck, OnUpdateGameSession) to support all GameLift features

